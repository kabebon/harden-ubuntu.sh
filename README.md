# harden-ubuntu.sh

🔐 **Одноразовый скрипт для безопасной начальной настройки свежего Ubuntu-VPN сервера**

Что делает скрипт одним запуском:

- Запрашивает желаемое имя пользователя  
- Запрашивает новый порт SSH  
- Генерирует свежую пару ed25519 ключей  
- Показывает приватный ключ → требует подтверждения, что вы его скопировали  
- Создаёт/обновляет пользователя, добавляет публичный ключ  
- Отключает вход по паролю и root-доступ  
- Меняет порт SSH в конфигурации  
- Включает TCP BBR (если поддерживается ядром)  
- Устанавливает и настраивает fail2ban под новый порт  
- Настраивает UFW (разрешает новый SSH-порт + 80/443)  
- При прерывании (Ctrl+C) или ошибке пытается откатить опасные изменения

### ❗Важные предупреждения ❗

- **Оставайтесь в текущей сессии root**, пока не проверите подключение по новому пользователю и порту в **отдельном терминале**  
- **Скопируйте приватный ключ сразу** — он показывается только один раз и удаляется  
- Скрипт **не предназначен** для повторного запуска на уже настроенных серверах (может перезаписать ключи)

### Как запустить (рекомендуемый безопасный способ)

```bash
# 1. Скачиваем скрипт
curl -fsSL https://raw.githubusercontent.com/kabebon/harden-ubuntu.sh/main/harden-ubuntu.sh -o harden.sh

# 2. Делаем исполняемым
chmod +x harden.sh

# 3. Запускаем от root
sudo ./harden.sh
